from turtle import Turtle

kenar = Turtle()

kenar.forward(180) # 180 birim ileriye git
kenar.left(90) # 90 derece sola dön
kenar.forward(90) # 90 birim ileriye git
kenar.left(90) # 90 derece sola dön
kenar.forward(180) # 180 birim ileriye git
kenar.left(90) # 90 derece sola dön
kenar.forward(90) # 90 birim ileriye git